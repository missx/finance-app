import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    footerNavbar: {
        alignSelf: 'center'
    },
    buttonGrp: {
        height: 55,
        width: "100%",
        marginBottom: 0,
        padding: 0,
        
    }
});

export default styles;