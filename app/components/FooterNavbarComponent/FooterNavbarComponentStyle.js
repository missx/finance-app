import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    footerNavbar: {
        flexDirection: 'row'
    },
    buttonGrp: {
        height: 55,
        alignItems: "stretch",
        width: "100%",
        marginBottom: 0
    }
});

export default styles;